# Teleprompter MVP

Edge-first teleprompter with client-side ASR (Whisper-tiny via transformers.js) and Web Speech API fallback.

This repo contains an initial MVP: worker-based ASR, a React PWA scaffold (Vite + TypeScript), and a basic teleprompter UI.

See src/ for code.
